<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Status/navigation bars are deliberately left
         at the platform default (no tint overrides): the system guarantees
         readable icon contrast there, which our app-colored bars did not. -->
    <style name="Theme.app" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
</resources>
